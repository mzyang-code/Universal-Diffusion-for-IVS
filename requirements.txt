# Core
numpy>=1.24
pandas>=2.0
scipy>=1.10
torch>=2.0            # CUDA build; training and sampling assume GPUs
# Data preparation
pyarrow>=12.0         # streams the raw / cleaned option-quote parquet files
joblib>=1.3           # per-day and per-ticker parallelism
tqdm>=4.60
requests>=2.25        # FRED 3-month T-bill fallback rate
# Training-loss plots
matplotlib>=3.7
